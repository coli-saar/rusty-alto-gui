name: CI

on:
  push:
    branches: ["main"]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, windows-latest, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
        with:
          path: rusty-alto-gui
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y libgtk-3-dev libxkbcommon-dev libwayland-dev
      - name: Check formatting
        working-directory: rusty-alto-gui
        run: cargo fmt --check
      - name: Test
        working-directory: rusty-alto-gui
        run: cargo test --locked
      - name: Clippy
        working-directory: rusty-alto-gui
        run: cargo clippy --locked --all-targets --all-features -- -D warnings
      - name: Release build
        working-directory: rusty-alto-gui
        run: cargo build --release --locked
      - name: Reject future-incompatible block dependency
        working-directory: rusty-alto-gui
        shell: bash
        run: |
          if cargo tree --locked -i block >/dev/null 2>&1; then
            echo "future-incompatible block dependency is present"
            exit 1
          fi
